export LC_ALL=en_US.UTF-8       #! Super important

# The following lines were added by compinstall
zstyle ':completion:*' insert-tab false
zstyle ':completion:*' auto-description 'specify: %d'
zstyle ':completion:*' completer _expand _complete _ignored _correct _approximate
zstyle ':completion:*' expand prefix suffix
zstyle ':completion:*' format 'Completing %d'
zstyle ':completion:*' group-name ''
zstyle ':completion:*' list-colors ''
zstyle ':completion:*' list-suffixes true
zstyle ':completion:*' matcher-list '' 'm:{[:lower:][:upper:]}={[:upper:][:lower:]}' 'r:|[._;]=** r:|=**' 'l:|=* r:|=*'
zstyle ':completion:*' menu select=1
zstyle ':completion:*' select-prompt %SScrolling active: current selection at %p%s
zstyle :compinstall filename '${HOME}/.zshrc'

autoload -Uz compinit; compinit

# Lines configured by zsh-newuser-install
HISTFILE=~/.histfile
HISTSIZE=10000
SAVEHIST=10000

setopt EXTENDED_HISTORY       # save timestamp + duration for each command
setopt INC_APPEND_HISTORY     # write each command to HISTFILE as soon as it's run (not just on exit)
setopt SHARE_HISTORY          # import new commands from HISTFILE + share across sessions live
setopt HIST_EXPIRE_DUPS_FIRST # when trimming, drop dupes before unique commands
setopt HIST_IGNORE_DUPS       # don't record a command if it's the same as the previous one
setopt HIST_IGNORE_ALL_DUPS   # remove older dupe when a new identical command is added
setopt HIST_FIND_NO_DUPS      # don't show dupes when searching history
setopt HIST_SAVE_NO_DUPS      # don't write dupes to the history file
setopt HIST_VERIFY            # let you review a !-expanded command before running it
# setopt autocd extendedglob nomatch notify shwordsplit
setopt autocd extendedglob nomatch notify
# unsetopt beep
bindkey -e

autoload -U +X bashcompinit && bashcompinit

# Use neovim for command editing (chord ctrl x + ctrl e)
export EDITOR="nvim"
export VISUAL="nvim"
autoload -Uz edit-command-line
zle -N edit-command-line
bindkey -M emacs '^X^E' edit-command-line
bindkey -M viins '^X^E' edit-command-line
bindkey -M vicmd '^X^E' edit-command-line
bindkey -M vicmd v edit-command-line

# Personal addition
if [ -x /usr/bin/dircolors ]; then
    test -r ~/.dircolors && eval "$(dircolors -b ~/.dircolors)" || eval "$(dircolors -b)"
    alias ls='ls --color=auto'
    alias dir='dir --color=auto'
    alias vdir='vdir --color=auto'

    alias grep='grep --color=auto'
    # export GREP_COLORS='ms=01;31'
    alias fgrep='fgrep --color=auto'
    alias egrep='egrep --color=auto'
fi

alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
alias lla='ls -la'
command -v tree >/dev/null && alias lt='tree'

# personalized aliases
alias c=clear
alias k=kubectl
alias h=helm
alias s=stern
alias g=git
alias v=nvim
alias lg=lazygit
alias dif="diff --color=always -y"
alias less="less -R"
alias dkr=docker
alias ld=lazydocker
alias kx='f() { [ "$1" ] && kubectl config use-context $1 || kubectl config current-context ; } ; f'
alias kn='f() { [ "$1" ] && kubectl config set-context --current --namespace $1 || kubectl config view --minify | grep namespace | cut -d" " -f6 ; } ; f'
alias kd='kubectl config unset current-context'
alias sct='f() { [ "$1" ] && (head /dev/urandom | tr -dc A-Za-z0-9 | head -c "$1" ; echo '') || (head /dev/urandom | tr -dc A-Za-z0-9 | head -c 13 ; echo '') ; } ; f'
alias zsh_history_fix='f() { mv ~/.histfile ~/.histfile.bad ; strings ~/.histfile.bad ~/.histfile ; fc -R ~/.histfile ; rm ~/.histfile.bad ; } ; f'
alias dateUpdateWsl='sudo ntpdate time.windows.com'
alias yq='yq -C'
alias jq='jq -C'
command -v go-task >/dev/null && alias task='go-task'

function yless() {
    yq . -C | less -R
}
alias lessy=yless
function jless() {
    jq . -C | less -R
}
alias lessj=jless
alias uidgen='cat /proc/sys/kernel/random/uuid'

function _ksec_list() {
  kubectl --context "$1" -n "$2" get secrets
}

function ksec() {
  local secret=""
  local context=""
  local namespace=""
  local field=""

  while [[ $# -gt 0 ]]; do
    case $1 in
      -c|--context)   context=$2;   shift 2 ;;
      -n|--namespace) namespace=$2; shift 2 ;;
      -f|--field)     field=$2;     shift 2 ;;
      *)              secret=$1;    shift   ;;
    esac
  done

  if [[ -z $context ]]; then
    context=$(kubectl config current-context)
  fi

  if [[ -z $namespace ]]; then
    namespace=$(kubectl config view -o jsonpath="{.contexts[?(@.name==\"$context\")].context.namespace}")
  fi

  if [[ -z $secret ]]; then
    echo "Usage: ksec [-c context] [-n namespace] <secret-name> [-f field]"
    echo ""
    echo "Fecthing available secrets: ..."
    _ksec_list $context $namespace | tee >(less)
    return 1
  fi

  local -a cmd=(kubectl --context=$context -n $namespace get secret $secret -o json)
  if ! output=$("${cmd[@]}" 2>&1); then
    echo "Error: secret '$secret' not found in context '$context' / namespace '$namespace'"
    echo ""
    echo "Fecthing available secrets: ..."
    kubectl --context=$context -n $namespace get secrets --no-headers -o custom-columns="NAME:.metadata.name" | tee >(less)
    return 1
  fi

  if [[ -n $field ]]; then
    "${cmd[@]}" | jq -r ".data[\"$field\"]" | base64 -d | tee >(wl-copy --trim-newline)
  else
    "${cmd[@]}" | jq -Mr '.data | map_values(@base64d)' | tee >(wl-copy --trim-newline)
  fi
}

# Add | as word boundary when deleting words backward 
export WORDCHARS="${WORDCHARS}|"

# Detect if using Homebrew or native package manager
if [ -d "/home/linuxbrew/.linuxbrew" ]; then
    # Homebrew installation
    export PATH="/home/linuxbrew/.linuxbrew/opt/rustup/bin:$PATH"
    export PATH="/home/linuxbrew/.linuxbrew/bin:$PATH"
    ZSH_HISTORY_SUBSTRING_SEARCH="/home/linuxbrew/.linuxbrew/share/zsh-history-substring-search/zsh-history-substring-search.zsh"
    ZSH_AUTOSUGGESTIONS="/home/linuxbrew/.linuxbrew/share/zsh-autosuggestions/zsh-autosuggestions.zsh"
    POWERLEVEL10K="/home/linuxbrew/.linuxbrew/share/powerlevel10k/powerlevel10k.zsh-theme"
else
    # Arch Linux or other system package manager
    ZSH_HISTORY_SUBSTRING_SEARCH="/usr/share/zsh/plugins/zsh-history-substring-search/zsh-history-substring-search.zsh"
    ZSH_AUTOSUGGESTIONS="/usr/share/zsh/plugins/zsh-autosuggestions/zsh-autosuggestions.zsh"
    POWERLEVEL10K="/usr/share/zsh-theme-powerlevel10k/powerlevel10k.zsh-theme"
fi

# keep PATH free of duplicates when this file is re-sourced
typeset -U path PATH

export PATH="${HOME}/.local/bin:$PATH"
export PATH="$PATH:$HOME/go/bin"
export PATH="$PATH:/opt/ApacheDirectoryStudio"
export PATH="/usr/local/go/bin:${HOME}/.local/share/applications:${PATH}"
export PATH="${KREW_ROOT:-$HOME/.krew}/bin:$PATH"
export PATH="$PATH:$HOME/minio-binaries/"

[ -f "$ZSH_HISTORY_SUBSTRING_SEARCH" ] && source "$ZSH_HISTORY_SUBSTRING_SEARCH"
[ -f "$ZSH_AUTOSUGGESTIONS" ] && source "$ZSH_AUTOSUGGESTIONS"

# To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
[ -f "$POWERLEVEL10K" ] && source "$POWERLEVEL10K"
[ -f ~/.p10k.zsh ] && source ~/.p10k.zsh

# ========== WSL only (not native Linux)
if grep -qi microsoft /proc/version 2>/dev/null; then
    export MESA_D3D12_DEFAULT_ADAPTER_NAME=AMD

    alias cl=clip.exe
    alias clip=clip.exe
    alias pc='pwd|cl'
    alias dateUpdateWsl='sudo ntpdate time.windows.com'

    # for windows terminal to keep_current_path on split
    keep_current_path() {
      printf "\e]9;9;%s\e\\" "$(wslpath -w "$PWD")"
    }
    precmd_functions+=(keep_current_path)
fi
# ======= END WSL

export KUBECONFIG=$HOME/.kube/config
export KUBE_EDITOR="nvim"

command -v fzf >/dev/null && source <(fzf --zsh)

# Development toolchains (python/node/go/rust) are managed by mise + rustup,
# see task/config.yaml
if command -v mise >/dev/null 2>&1; then eval "$(mise activate zsh)"; fi

[ -f "$HOME/.cargo/env" ] && source "$HOME/.cargo/env"

# devops tool completions
if command -v kubectl >/dev/null 2>&1; then
    source <(kubectl completion zsh)
    compdef k=kubectl
fi

if command -v helm >/dev/null 2>&1; then
    source <(helm completion zsh)
    helm plugin list 2>/dev/null | grep -q '^diff' && source <(helm diff completion zsh)
    compdef h=helm
fi

if command -v stern >/dev/null 2>&1; then
    source <(stern --completion=zsh)
    compdef s=stern
fi

if command -v mc >/dev/null 2>&1; then
    complete -o nospace -C "$(command -v mc)" mc
fi

if command -v direnv >/dev/null 2>&1; then eval "$(direnv hook zsh)"; fi

if command -v niri >/dev/null 2>&1; then source <(niri completions zsh); fi

if command -v wt >/dev/null 2>&1; then eval "$(command wt config shell init zsh)"; fi

if command -v herdr >/dev/null 2>&1; then source <(herdr completion zsh); fi

#THIS MUST BE AT THE END OF THE FILE FOR SDKMAN TO WORK!!!
export SDKMAN_DIR="$HOME/.sdkman"
[[ -s "$HOME/.sdkman/bin/sdkman-init.sh" ]] && source "$HOME/.sdkman/bin/sdkman-init.sh"


