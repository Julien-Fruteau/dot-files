#!/usr/bin/env bash
# Refuse un commit contenant un secret détecté dans les changements indexés.
# Contournement ponctuel et assumé : git commit --no-verify

set -euo pipefail

exec gitleaks git --pre-commit --redact --staged --verbose
